export enum PgfStatus {
  DRAFT = 'draft',
  PENDING_REVIEW = 'pending_review',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  UPLOADED = 'uploaded',
  PARSED = 'parsed',
}

export enum PgfUnit {
  WEEK = 'week',
  MONTH = 'month',
}
